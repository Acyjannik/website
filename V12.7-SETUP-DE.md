ACY V12.8 — Profile-Link-Fix

Problem aus dem Screenshot: Die Auth-Session ist vorhanden, aber für den eingeloggten User wird kein `profiles`-Datensatz gefunden. Dadurch fällt das Dashboard auf „Hey, Member“ und 0 XP zurück.

Fix:
1. `supabase/ensure_my_profile_v12.7.sql` einmal im Supabase SQL Editor ausführen.
2. V12.8 deployen.
3. Seite mit Strg+F5 laden.

Das SQL repariert auch bereits vorhandene Auth-User ohne Profile-Zeile. Neue User können ihr Profil bei Bedarf automatisch über `ensure_my_profile()` anlegen.
Build: ACY BUILD V12.8
