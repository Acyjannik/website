# ACYJANNIK V15.4 – Mobile Pet Controls & Private Messages Dock

- Mobile Pet management buttons use a dedicated two-column layout with a full-width release action.
- Bottom dock replaces public Chat with **Privat** / Private Messages.
- Dock navigation now opens the target `<details>` panel automatically.
- Quests, Private Messages and Glücksrad open their corresponding accordion when selected.
- Private Messages uses `#club-messages` and keeps its unread badge.
- No database migration required.
