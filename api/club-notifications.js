export default async function handler(req,res){res.setHeader('Cache-Control','no-store, max-age=0');const url=process.env.SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)return res.status(503).json({error:'Service is not configured.'});const auth=req.headers.authorization||'';if(!auth.startsWith('Bearer '))return res.status(401).json({error:'Unauthorized'});try{const who=await fetch(`${url}/auth/v1/user`,{headers:{apikey:key,Authorization:auth}});if(!who.ok)return res.status(401).json({error:'Invalid session.'});const user=await who.json();if(req.method==='POST'){const d=await fetch(`${url}/rest/v1/profiles?id=eq.${user.id}`,{method:'DELETE',headers:{apikey:key,Authorization:`Bearer ${key}`}});if(!d.ok)return res.status(500).json({error:'Profile deletion failed.'});const a=await fetch(`${url}/auth/v1/admin/users/${user.id}`,{method:'DELETE',headers:{apikey:key,Authorization:`Bearer ${key}`}});if(!a.ok)return res.status(500).json({error:'Auth account deletion failed.'});return res.status(200).json({deleted:true})}if(req.method!=='GET')return res.status(405).json({error:'GET only'});const r=await fetch(`${url}/rest/v1/club_notifications?user_id=eq.${user.id}&select=*&order=created_at.desc&limit=30`,{headers:{apikey:key,Authorization:`Bearer ${key}`},cache:'no-store'});const t=await r.text();if(!r.ok)return res.status(500).json({error:t||'Could not load notifications.'});return res.status(200).json({notifications:t?JSON.parse(t):[]})}catch(e){console.error(e);return res.status(500).json({error:'Notification service failed.'})}