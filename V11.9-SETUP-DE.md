ACY V11.9 — Progression & Achievements

Behebt:
- Der Achievements-Bereich bleibt nicht mehr leer, wenn eine direkte Supabase-Abfrage durch RLS blockiert wird.
- Die serverseitige Achievement-Prüfung wird vor dem Rendern genutzt.
- Der Katalog zeigt immer gesperrte UND freigeschaltete Achievements.
- 30 Club-Level werden dynamisch dargestellt statt nur der ersten 12.
- Höhere Level bekommen zunehmend stärkere Glow-/Tier-Effekte.
- Die Level-Karten markieren das aktuelle Level deutlich.

SQL:
Keine neue SQL-Migration für diese UI-/Ladefixes nötig.

Deployment:
1. V11.9 deployen.
2. Strg+F5 / Cache leeren.
3. Supabase muss weiterhin die bereits vorhandenen Achievement-/Progression-SQLs aus den vorherigen Versionen enthalten.
